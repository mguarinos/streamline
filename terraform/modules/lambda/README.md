# Module: lambda

Provisions the status API Lambda function (TypeScript, Node 22), its `live` alias, a function URL with `AWS_IAM` auth, the SSM parameter for stream state, and the EventBridge rule that triggers the state handler on IVS channel events.

<!-- BEGIN_TF_DOCS -->
<!-- END_TF_DOCS -->
