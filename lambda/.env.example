IVS_PLAYBACK_URL=https://test-streams.mux.dev/x36xhzz/x36xhzz.m3u8
IVS_DVR_ENABLED=true
AWS_REGION=eu-west-1
SSM_STREAM_STATE_PARAM=/streamline/local/stream-state
# Bypass SSM for local development (set to "idle" or "live").
# Without real AWS credentials the SSM call would fail; this avoids it.
STREAM_STATUS_MOCK=idle
